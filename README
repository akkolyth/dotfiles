.dotfiles

Installation

To set up these dotfiles on your system, follow these steps:

1. Clone the repository to your local machine:

   git clone https://github.com/akkolyth/dotfiles.git ~/.dotfiles

2. Navigate to the directory:

   cd ~/.dotfiles

3. Run the installation script with elevated privileges:

   sudo -E ./install.sh
